console.log("Holaa Mundo, ....Hoy es 7 de octubre...")


/*Practicas de hoy descargar la libreria de tailwindcsss
con el comando "npm install -D tailwindcss"
Despues se instalara toda el paquete para verificar que la paqueteria fue instalada ve a package.json  
en devDependenciaes""tailwindcss:3.3.3"
*/


/*nuevo comando que se ejecutara npx tailwindcss init
despues de ejecutar este comando te crea un archivo tainwind.config.js 
en el archivo tenemos un apartado de propiedades
en content: ponemos */
//content:["./src/**/*.{html,js}"],

/*dentro de nuestra carpeta src creamos una carpeta llamada input.css y dentro de ese archivo se coloca lo siguiente
despues en el jscon agrego    "css-compiler":"npx tailwindcss -i ./src/input.css -o ./dist/output.css --watch"
luego ejecuto el comando de npm run css-compiler y te crea una carpeta que se llama dist y un archivo output.css
despues se hace la vinculacion a ese texto de salida despues dentro de la carpeta de views creamos otra carpeta llamda layout 
y dentro de layout creamos un archivo llamada index.pug y ahi ya ejecute los comandos que pidio */

/*descargara la extencion tailwind css intellisense a visual studio code */

/*luego nos vamos a nuestro archivo de generalRoutes
luego se ejecuta el comando npm run css-compiler

creamos una carpeta dentro de views se crea otra carpeta llamda AUTH 
creamos un nuevo archivo dentro de routs llamda userRouts.js y hacemos todas lo que ya hice xdxd
DEP*/






/*
@tailwind base;
@tailwind components;
@tailwind utilities;
 */


/*ANOTACIONES DEL DIA DE HOY */